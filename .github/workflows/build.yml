name: Build Flet Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Flet and Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flet

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Build APK
        run: |
          # استخدام -v للتتبع المفصل (Verbose)
          # استخدام --include-packages لضمان تضمين flet وتبعياتها
          flet build apk -v --include-packages flet,urllib3,certifi,mutagen,pycryptodomex

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: R-plus-apk
          path: build/apk/*.apk
